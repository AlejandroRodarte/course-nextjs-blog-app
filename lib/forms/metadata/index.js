import generateFromSpec from "./generate-from-spec";
import updateFormMetadata from "./update-form-metadata";
import updateInputMetadata from "./update-input-metadata";

const metadata = {
  generateFromSpec,
  updateFormMetadata,
  updateInputMetadata,
};

export default metadata;
